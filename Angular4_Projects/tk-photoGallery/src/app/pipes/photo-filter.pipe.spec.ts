import { PhotoFilterPipe } from './photo-filter.pipe';

describe('PhotoFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PhotoFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
