var abc = ['a', 'b', 'z', 'dS'];

var cExists = abc.indexOf('c') > -1; // does c exist in array?

if (!cExists) {
    abc.push('C IS IN NOW BRUH');  // if c doesn't exist in array push this string
}

// console.log(abc.indexOf('c'));
console.log(abc);
